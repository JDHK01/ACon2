# adjust

- `kf.py`
  - 去掉了 `obs` 属性
  - 修正了 `predict` 方法, 增加了状态空间向观测空间的转换, $z=Hx+v$, 提高可扩展性
  - 矩阵乘法使用 @ 替代 *, 提高可扩展性
  - 使用 `.T` 替代 `np.transpose`, 更简洁
  - 修正了 `update_noise` 方法, 原方法中使用了上一次的状态值与当前的观测值进行在线学习. 由于当前的F和H都是[[1]]且针对cov进行了特殊处理, 所以结果上没有问题. 但是缺乏明确的含义, 这里我进行了修正.使用 `predict` 方法直接得到预测值
  - python内置的 `max` 不支持数组操作, 仅仅适用于一维的情况. 这里该用 `np.maximum`

- `sigma.py`
  - init中的args多余为了避免麻烦, 设为了可选参数

- `mvp.py`
  - 将 `norm_func` 方法放到了init里面(原来在 `reset` 方法里), 语意更清
